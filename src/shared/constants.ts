export const DEFAULT_LEADER_KEY = "`";

/**
 * Reserved for the built-in leader+r "start recording" chord. Never
 * assignable to a user binding - see the reservation check in
 * src/content/recorder.ts.
 */
export const RECORD_KEY = "r";
