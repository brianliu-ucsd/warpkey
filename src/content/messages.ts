/** Message sent from the popup to arm recording mode on the active tab's content script. */
export const ARM_RECORD_MESSAGE = "warpkey:arm-record";

/** Message sent from the popup asking the active tab's content script to resolve current on-page text for each binding, keyed by binding id. */
export const RESOLVE_LABELS_MESSAGE = "warpkey:resolve-labels";
